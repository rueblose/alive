## Что дальше

Ближайшее по ценности:

* **Список потерянных файлов** внутри сета, а не только счётчик, плюс попытка найти файл
  по имени в известных папках и предложить починку.
* **Чистка диска:** бэкапы и дубли сэмплов между проектами, плюс размер папки проекта
  прямо в таблице.
* **Файлы проекта в инспекторе:** дерево папки, где видно, на что из неё есть ссылки из
  сета, а что лежит мёртвым грузом.
* **Прослушать проект:** кнопка play у имени сета, играющая последний рендер из корня
  папки проекта.


## Группировка сетов в одной папке

Glass Horizon                         12 sets
├─ Glass Horizon.als                  2026-08-01
├─ Glass Horizon final.als            2026-08-03
├─ Glass Horizon final 2.als          2026-08-05
└─ Backup                              8 sets

По клику раскрываются конкретные .als.

## Главная страница в стиле DaVinci Resolve

Главная страница должна быть не аналитической, а визуальной:

Recent Projects 
[ Glass Horizon ] [ Night Drive ] [ Untitled Idea ]
[ Demo 143      ] [ Album Track 5 ] [ Vocal Tests ]

На плитке:
превью аранжировки;
название проекта;
дата последнего изменения;
кнопка Play, если найден рендер.

Recent
Проекты, которые недавно открывались через Manager или были изменены.

Pinned
Закреплённые проекты. Это полезнее, чем полноценные теги: одно нажатие и проект всегда под рукой.

All projects
Переход в основной каталог.

Я бы обязательно добавил Pin project, но не стал пока делать сложную систему избранного, рейтингов и коллекций.

## Watch Mode

Кнопка Random Project




## Missing Files Center — ПОКА В ПЛАНАХ. НА ДАЛЕКОЕ БУДУЩЕЕ
Это должно быть отдельное окно, а не попытка запихнуть всё в панель деталей.
Но я бы не начинал сразу с полноценного менеджера файлов и глобальной индексации всех дисков. Это быстро превращается в отдельный продукт.
Разумная первая версия
Окно:
File                  Used in       Expected path       Status
--------------------------------------------------------------
vocal_take_07.wav     Vocal 1       Samples/Vocals      Missing
bass_loop.wav         Bass           ../../Loops         Missing

Кнопки:
Search in folder...
Search in project folders
Show expected path
Open containing folder
Copy report
Rescan
То есть первая версия может быть диагностической, без автоматического изменения .als.

Индексация файлов
пользователь выбирает папки для поиска;
менеджер сканирует только их;
сохраняет индекс имён, размеров и дат;
глубокий анализ запускается только по запросу;
индекс обновляется при необходимости.

Например, пользователь добавил:
text
E:\Samples
F:\Old Projects
G:\Backup
Менеджер не обязан каждый запуск заново проходить несколько терабайт. Достаточно кэшировать:

text
имя → путь, размер, дата изменения
Хэши считать только у кандидатов одинакового размера.

Поиск похожего аудио
Я бы разделил две задачи:

Точное восстановление
Файл найден по:
имени;
расширению;
размеру;
пути;
возможно, длительности.
Это относительно безопасно.

Замена на похожий файл
Это уже не восстановление, а творческая подмена. Например, заменить потерянный kick на похожий kick. Автоматически делать такое опасно.

Для первой версии можно только показывать кандидатов:

text
Possible matches:

97%  kick_07.wav
82%  kick_07_new.wav
64%  kick_alt.wav
А саму замену оставлять пользователю или делать отдельным ручным действием.

Сравнение может идти по:

нормализованному имени;
длительности;
числу каналов;
sample rate;
размеру;
аудиосигнатуре.
Но я бы не начинал с автоматической замены внутри .als. Даже если ID клипов не обязательно сломаются, FileRef может содержать дополнительные поля: хэш, размер, сведения об анализе. Сначала нужно научиться надёжно создавать копию .als, менять ссылку, пересобирать gzip/XML и проверять открытие в Live.

**Сделано (по-другому):** помощник по восстановлению — `Ctrl R`, `src/RescueDialog.cs`. Он
ничего из .als не удаляет: в КОПИИ сета подменяется только идентификатор плагина
(`Uid`/`UniqueId`), и Live показывает его как «плагин не найден», а всё остальное — узлы,
цепочки, автоматизация, блоб пресета — остаётся байт в байт. Диагноз обычно даже не требует
проб: журнал Live пишет `VST3: Going to restore: X` перед тем, как умереть внутри X.

Ниже — про удаление плагина ИЗ сета, и это по-прежнему не стоит делать.

Особенно не стоит пока пытаться удалять плагины из .als. Плагин связан не только с одним узлом, но может иметь:

состояние устройства;
связи с цепочками;
automation;
ID;
параметры;
соседние узлы в DeviceChain.
Это хорошая тема на потом, но точно не ближайшая функция.


