# Ableton Options

Редактор `Options.txt` для Ableton Live: галочки вместо ручной правки текстового файла.
Все известные опции собраны в одном списке — с описанием, категорией, диапазоном значений
и пометкой, насколько опции можно доверять.

Цель — **Live 12.4.3** (список опций также покрывает Live 11 и помечает то, что удалено).

## Что умеет

- Сам находит все установленные версии Live в `%APPDATA%\Ableton\Live x.x.x\Preferences`
  и переключается между ними; стабильные версии идут выше бет, папку можно указать вручную.
- **153 опции** с описанием: включить/выключить галочкой, у числовых и текстовых — поле значения
  с проверкой диапазона (кликаешь по полю и правишь на месте).
- Поиск по названию, описанию и имени опции + фильтр по категории.
- Строка опции (`-_PluginAutoPopulateThreshold=64`) показывается прямо под заголовком — видно,
  что именно уедет в файл.
- «Показать файл» — предпросмотр итогового `Options.txt` до записи.
- Перед записью автоматически делается резервная копия `Options.txt.bak-ГГГГММДД-ЧЧММСС`.
- Опции, которых нет в каталоге (свои, из будущих версий Live), **не теряются** — читаются из
  файла, сохраняются обратно и правятся через кнопку «Свои опции…».
- Если не выбрано ничего, файл удаляется — Live запускается в чистом состоянии.

Горячие клавиши: `Ctrl+F` — поиск, `Ctrl+S` — сохранить, `Esc` — закрыть.
Изменения вступают в силу только после перезапуска Live.

## Язык

Интерфейс по умолчанию английский — переключатель `EN | RU` в правом верхнем углу.
Переводится всё, включая названия и описания всех 153 опций. Выбор запоминается в
`%APPDATA%\AbletonOptions\ui.cfg`.

## Про внешний вид

Окно безрамочное, со скруглёнными углами и «матовым стеклом» — одна подложка во всё окно,
без внешней рамки. При запуске снимается кадр рабочего стола, сильно уменьшается,
размывается box-blur'ом в три прохода и растягивается обратно: получается настоящее
размытие того, что за окном, без недокументированных DWM-хаков. Кадр берётся **один раз
до показа окна**, поэтому в него не попадает само приложение; при перетаскивании окна
подложка честно сдвигается по снимку.

Размытая картинка собирается один раз во весь виртуальный экран, и дальше каждый контрол
копирует свой кусок **один-в-один, без масштабирования**. Это принципиально: пересчёт с
интерполяцией на каждую перерисовку стоил ~7 мс только на фон списка, копия — ~1,4 мс
(замерено `PerfTest`). Наведение мыши перерисовывает одну строку, а не весь список,
а высоты строк кешируются, чтобы набор текста в поиске не пересчитывал их все.

Список опций — не 153 отдельных контрола WinForms, а один owner-drawn контрол со своей отрисовкой,
прокруткой и попаданиями мыши: иначе такой интерфейс не выглядел бы так и заметно тормозил.

Окно тянется за любое пустое место шапки и подвала, размер меняется за края. Координаты
в `WM_NCHITTEST` приводятся к `short`: без этого на мониторе слева или сверху от основного
(отрицательные экранные координаты) окно нельзя было ухватить — везде показывался курсор
изменения размера.

## Сборка и запуск

Ничего ставить не нужно: сборка идёт компилятором C#, который уже входит в состав
.NET Framework 4.x (он есть в любой Windows 10/11).

```bash
cd "C:\Users\RUE\Documents\Code\Ableton Options" && cmd /c build.cmd
```

Результат — `bin\AbletonOptions.exe`, один самодостаточный файл, права администратора не нужны.

## Пометки надёжности

Ableton официально документирует лишь несколько опций — остальное держится на знаниях
сообщества и может исчезнуть в любом обновлении. Поэтому у каждой опции стоит бейдж:

| Бейдж | Что значит |
|---|---|
| `официально` | описано в базе знаний Ableton |
| `Live 12` | добавлено/подтверждено в Live 12 |
| *(без бейджа)* | рабочая опция Live 11/12 по данным сообщества |
| `служебная` | внутренняя, для отладки и тестов Ableton — обычному пользователю не нужна |
| `не работает` | удалена в Live 11, в Live 12 не действует (показывается по галочке «Устаревшие») |

Служебные и устаревшие по умолчанию скрыты — включаются галочками в шапке.

Проверено: имена опций **не лежат строками внутри `Ableton Live 12 Suite.exe`** и вообще
нигде в установке Live (Ableton хеширует их при компиляции), поэтому автоматически вытащить
полный список из бинарника нельзя. Каталог собран из официальной статьи Ableton и сверенных
публичных списков, а затем проверен на живом Live 12.4.3.

## Сверка с самим Live

Ключевое наблюдение: **Live сам сообщает, какие опции он не понимает.** При запуске он
разбирает `Options.txt` на первой секунде и на каждое незнакомое имя пишет в
`Preferences\Log.txt`:

```
info: Message Box: The options file [Options.txt] contains an unknown option
'-MidiClockSlave' which will be ignored.
```

Приложение читает этот журнал и помечает такие опции красным бейджем «твой Live её не
понимает», а при сохранении отдельно предупреждает о них. Это точнее любого списка из
интернета и работает для любой версии Live — каталог сам себя проверяет на твоей машине.

Важная тонкость: строки `Options: -X` в том же логе — это **эхо файла**, они пишутся и для
принятых, и для отвергнутых опций. Доверять можно только сообщениям об ошибке.

Что дала проверка на Live 12.4.3:

| Результат | Опции |
|---|---|
| **77 подтверждены** — приняты без единого предупреждения | весь пользовательский набор: `-ShowDeviceSlots`, `-EnableArmOnSelection`, `-NoAutoArming`, `-RestrictAudioCalculationToPerformanceCores`, `-_Feature.Browser.AsyncLoading` и т.д. |
| **9 отвергнуты** — удалены из каталога либо помечены | `-VstNoLocalDir`, `-VstNoScanSkip`, `-NoVstStartupScan`, `-NoMidiMonitorLatencyCompensation`, `-MidiClockSlave`, `-ShowBackToArrangementOnMasterTrack`, `-ThinningAggressiveness`, `-EnableMapToSiblings`, `-ShowToolsMenu` |
| **не проверены** | остальные устаревшие и служебные — они по умолчанию скрыты, и в каталоге честно помечены, а не выданы за рабочие |

Первые восемь до сих пор числятся рабочими во всех публичных списках, включая те, что указаны
в источниках ниже, — эти списки составлялись для Live 11 и устарели.

## Источники

- [Options.txt file — Ableton](https://help.ableton.com/hc/en-us/articles/6003224107292-Options-txt-file) — официальная документация
- [Show Device Slots & Other Options.txt Commands in Live 12 — Sonic Bloom](https://sonicbloom.net/show-device-slots-options-txt-commands/) — опции, появившиеся в Live 12
- [Options.txt list of functions — Ableton Forum](https://forum.ableton.com/viewtopic.php?t=246702)
- [Список опций Live 11 — tubm.tokyo](https://tubm.tokyo/mixingmastering/options-txt-list/)
- [ibekso/medium — списки опций](https://github.com/ibekso/medium/tree/main/article_ressources/options_txt/lists)

## Структура

```
src/Catalog.cs         каталог опций: имя, тип, диапазон, категория, описание (EN + RU)
src/OptionsFile.cs     поиск папок Live, чтение/запись Options.txt, резервные копии
src/OptionListView.cs  список опций: отрисовка, прокрутка, правка значений
src/MainForm.cs        главное окно: стеклянная карточка, фильтры, сохранение
src/Controls.cs        свои контролы: кнопки, таблетки, сегменты, выпадающие списки
src/LiveLog.cs         сверка каталога с журналом самого Live
src/Backdrop.cs        снимок рабочего стола и размытие
src/Dialogs.cs         предпросмотр файла и правка своих опций
src/Theme.cs           палитра, шрифты, скруглённые примитивы
src/Loc.cs             переключение EN/RU
build.cmd              сборка
```

## Как добавить опцию

Одна строка в конце `Catalog.cs` — заголовок и описание сразу на двух языках:

```csharp
A("ИмяОпции", Kind.Flag, Stat.Ok, CatWorkflow,
  "Short title", "Короткий заголовок",
  "What it does and why you'd want it.", "Что она делает и зачем нужна.");
```

`Kind` — `Flag` / `Num` / `Text` / `Choice`; для `Num` можно задать `def`, `min`, `max`,
для `Choice` — `choices: "a|b|c"`.
