# The repository keeps LF and so does the working copy: otherwise an editor on Windows
# quietly converts a file to CRLF and the whole file lands in the diff instead of three edits.
* text=auto eol=lf

*.png  binary
*.ico  binary
*.emf  binary
*.exe  binary
