# Репозиторий держит LF, рабочая копия тоже: иначе редактор на Windows незаметно
# переводит файл в CRLF, и в diff попадает весь файл целиком вместо трёх правок.
* text=auto eol=lf

*.png  binary
*.ico  binary
*.emf  binary
*.exe  binary
